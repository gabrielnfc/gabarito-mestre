# Ledger — PBI: GM-5 · Epic: (b) — repo do plugin, sem onboarding   Plano: docs/superpowers/plans/2026-09-24-workflow-true-1.1.0-fase-5-skills.md   Branch: enabler/GM-5-skills @ d2c47af   Spec (autoridade): docs/superpowers/specs/2026-09-24-workflow-true-design.md
Rulings herdados: R1, R2, R3, R16, R18, F2-R8, F3-R1..R8, F4-R1..R10          Contexto externo: nenhum (só texto; sem banco, sem rede)

## LER PRIMEIRO — 2026-09-25
Fase 5: T22 (gabarito-instalar) · T23 (gabarito-conformidade) · T24 (agente implementador + comando doctor) — paralelas no grafo, seriais aqui enquanto `capacidade.mjs` der slot 1.
Fases 1–4 integradas em `main` (PR #4, `d2c47af`). Pré-condições medidas: `fluxo.md` 7 seções · 8 cards · 4 scripts · 3 templates novos · `ferramentas-mcp.json` · "Isolamento: porta"=1 · "antes de despachar"=1 · "Áreas e donos"=1 · "R30+"=1 · `onboarding-config.mjs` sem `--set` sai 1 · slots 1 (7 pesados).
Handoff da fase 4 (`docs/ledgers/GM-4.md`): C10 → T23; m8 → T24; m6 e m10 entram nesta fase (F5-R6, F5-R7); m7 e o resto → T25.

## Pre-flight — pares produz × consome
| Par | Produz × Consome | Achado |
|---|---|---|
| T22 × T9 | SKILL cita `cartao-sessao.mjs` e `--vincular` × cartão lê `.harness/fluxo-cache.json` | formato do cache é o do contrato |
| T22 × T5 | SKILL chama `onboarding-config.mjs --set/--settings` × CLI do contrato | flags ok; a saída ONB-7 no stdout (F2-R8) não está no brief → F5-R5 |
| T22 × Emenda #8 do mestre | `vinculoPbiEpic` é objeto `{tipo, nome}` × brief usa string (`"<resposta 5>"`, `"frontmatter"` no exemplo de verificação) | → F5-R5 |
| T22 × T21 (fase 4) | skill instala `PULL_REQUEST_TEMPLATE.md`, `CHANGELOG.md`, `backlog.md` (o `instalar.sh` não copia) × templates existem | ok |
| T22 × m10 (fase 4) | `templates/gabarito.yml` fixa `--base origin/main` × trunk `master` reprova sempre | → F5-R7 |
| T23 × T2 | conformidade cita `fluxo.md §n` × seções de `fluxo.md` | 7 seções, ordem FLX-1 |
| T23 × handoff C10 (fase 4) | texto "as skills do gabarito não refazem o que superpowers faz" tem de entrar na SKILL × brief não o traz | → F5-R4 |
| T24 × T20 | agente copia bloco Isolamento × `prompts.md` | cópia verbatim (Emenda #4), `diff` do bloco |
| T24 × m8 (fase 4) | prompts supõem worktree em todo dispatch × `referencia.md §3.1` só no paralelo | → F5-R6 |
| pré-condição do plano | `grep -c "Antes de despachar"` (maiúscula) = 1 × cabeçalho real "antes de despachar" | → F5-R8 |

## Rulings de pre-flight
F5-R1 — modelos (usuário: só opus e sonnet): opus em T22 (onboarding em quatro fases, maior juízo) e no review final; sonnet em T23, T24(+b) e nos reviews por task — custo-se-errado: uma rodada extra.
F5-R2 — T22–T24 em worktrees `wt/GM-5-<n>` só com slots ≥ 2 no dispatch; slot 1 → serial T22 → T23 → T24 neste worktree — custo-se-errado: tempo de relógio.
F5-R3 — implementadores não tocam `docs/ledgers/GM-5.md`; o orquestrador atualiza após cada review — custo-se-errado: nenhum.
F5-R4 — T23 cola o bloco C10 (handoff da fase 4, texto no plano da fase 4, "Handoff para as Fases 5 e 6"), adaptando "as skills" para "esta skill" — REQ-PKG-2: texto que saiu do núcleo tem destino — custo-se-errado: parágrafo a mais na skill.
F5-R5 — T22: (a) depois de cada `onboarding-config.mjs --set`, a skill decide pelo stdout — `[ONB-7] … atualizado` é sucesso; `[ONB-7] harness.config.json NÃO escrito — JSON inválido (fail-open declarado, G9)` e exit 1 fazem a skill parar, mostrar o stderr e corrigir (F2-R8, Emendas #11/#12); (b) `vinculoPbiEpic` é gravado como objeto `{ "tipo": "campo|relacionamento|frontmatter", "nome": "…" }` (Emenda #8 do mestre), inclusive no exemplo de verificação — custo-se-errado: onboarding que grava a forma velha e o cartão lê a nova.
F5-R6 — T24 ganha: (a) m8 — o bloco Isolamento do agente vale quando o dispatch é num worktree `wt/<PBI>-<n>` (paralelo); dispatch serial no checkout do orquestrador não usa porta/schema próprios (uma linha, sem mexer no bloco verbatim); (b) T24b — `templates/PULL_REQUEST_TEMPLATE.md` passa a listar `release` e `revert` entre os tipos sem PBI (m6; `tiposLivres` do contrato) — custo-se-errado: uma linha em cada arquivo.
F5-R7 — T22, fase 3 do onboarding (versionamento): se o trunk detectado não é `main`, a skill mostra a troca de `--base origin/main` por `--base origin/<trunk>` em `.github/workflows/gabarito.yml` e aplica com `Edit` só com ok (arquivo criado pela fase 1 do mesmo onboarding; R2 respeitado se já existia antes, aí só avisa) — m10 — custo-se-errado: CI do usuário com trunk `master` reprovando até ele editar.
F5-R8 — a pré-condição "Antes de despachar" com maiúscula não casa porque o cabeçalho canônico (fase 4, T20) é minúsculo; conferido com `grep -ci` = 1 — custo-se-errado: nenhum.
F5-R9 — trailer dos commits: `Co-Authored-By: Claude Opus 5.5 (1M context)` + `Claude-Session` (mesmo motivo de F4-R5) — custo-se-errado: atribuição.

## Progresso
2026-09-25 — GitHub configurado pelo orquestrador com autorização explícita do usuário (encerra a pendência de F4-R6): só squash, título da PR como cabeçalho do commit, corpo da PR como mensagem, branch apagada no merge; ruleset "main" (id 23998269) ativo em `main` — proíbe deleção e force-push, exige histórico linear, PR (0 aprovações — mantenedor único) e os checks `claude plugin validate --strict`, `doctor não se auto-detecta (M10)`, `hooks — corpora (bash ubuntu-latest)`, `hooks — corpora (bash macos-latest)`. O job "gates — 77 testes" fica fora da lista porque T25 renomeia; T25 acrescenta o nome novo ao ruleset.
Task T22: DONE (opus; e1753f4 feat, d637884 fix). `gabarito-instalar/SKILL.md` +317/−28: quatro fases com gatilho por `resolvidoEm`, tabela "Como ler a saída de onboarding-config.mjs" (F5-R5a, strings conferidas e rodadas em tmpdir: `atualizado`, `sem mudanças`, dry-run, exit 1, `NÃO escrito … G9`), `vinculoPbiEpic` objeto (F5-R5b; em `arquivos` = `frontmatter`/`epic`, a chave que `cartao-sessao.mjs` lê), troca de `--base` quando o trunk ≠ `main` só com ok e só se o arquivo foi criado nesta execução (F5-R7), `--vincular`. Desvios aceitos: rótulos do Apêndice seguem `AGENTS.md:241-247`; `capacidade.mjs --json` além de `--calibrar` (o `--calibrar` não traz medidas). Greps do brief 14/14; `validate --strict` ok.
Review (sonnet) REPROVADO: 1 MAJOR real — `allowed-tools` não cobria os comandos compostos (`if`/`[`/`grep`/`test`) que a skill manda rodar; em `claude -p` esses passos parariam. Dois MAJOR de cobertura (gatilho por fase e cláusula R2 do F5-R7 sem verificação automatizada — as mutações passam por todos os greps).
Ruling F5-R10 — rodada 1 só para o achado real; os dois de cobertura viram checagens do `ci.yml` de T25 com os greps prontos: `grep -cE '^\| (2 — fluxo \| `fluxo|3 — versionamento \| `versionamento|4 — orquestração \| `orquestracao\.modelo)\.resolvidoEm` \|'` = 3 e `grep -cF 'já existia** (saída `mantido`, ou a fase 1 não rodou nesta execução) → **não edite** (R2)'` = 1 sobre o SKILL.md (confirmados: caem com as mutações do review) — custo-se-errado: regressão no texto da skill passa até T25.
Fix round 1/5 (1 addressed, 0 open; e1753f4..d637884): "um comando por chamada" — sem `;`/`&&`/`||`/`if`; `test -e` antes, `cp -n` e `node -e` com `flag: "wx"` (recusam sobrescrever — R2 medido em tmpdir); `allowed-tools` + `Bash(test:*)`, `Bash(grep:*)`, sem `Bash(*)`. Re-review APROVADO: lista de comandos remontada do texto, todos cobertos; classificação do CODEOWNERS idêntica à anterior nos 4 casos. Minors diferidos: `--vincular` repetido acrescenta outra linha VINCULO; criação do CODEOWNERS sem captura de EEXIST (stack trace, R2 preservado); suspeita — `cartao-sessao.mjs:228-229` lê `epic` fixo em vez de `vinculoPbiEpic.nome` (T25/backlog). Não medido: execução ponta a ponta com AskUserQuestion e MCP real (T26).
Capacidade antes de T23/T24: slots 1 (7 pesados) → serial; T23 + T24 + T24b num só implementador (sonnet), um commit por task, review com veredito por task.
Task T23: DONE (sonnet; 8ad3ac9). `gabarito-conformidade/SKILL.md`: checklists de design/plano/cards/ADR, fio condutor (R19), contenda (R21), `PBI:` único, pré-conferência por `fluxo.resolvidoEm` (sem → AVISO, com → FALTA); bloco C10 colado em "Divisão de trabalho" (F5-R4). 9/9 greps do brief; `allowed-tools` sem `mcp__`; `validate --strict` ok.
Task T24: DONE (sonnet; 847c48c). Agente implementador com o bloco Isolamento verbatim de `prompts.md` (`diff` do bloco extraído = idêntico; `sed` com n=2 → `porta 3002 · schema wt_2 · namespace wt-2`) e a linha F5-R6(a) fora do bloco; comando do doctor explica `warn` ≠ FALTA e lista as 6 checagens novas que contam nível (bate com `NOVAS_1_1_0` menos `modelo-resolvido` e `tag-semver`).
Task T24b: DONE (sonnet; 5f5f9a6). `PULL_REQUEST_TEMPLATE.md` lista `release` e `revert` entre os tipos sem PBI (m6 da fase 4). Instalador 70 ok.
Review T23/T24/T24b (sonnet): T24 e T24b APROVADO (mutação do fecho de T24 derruba as 3 verificações; trocar uma palavra do bloco é pego pelo `diff`). T23 REPROVADO por 3 MAJOR: (1) a mutação do fecho (tirar "Sem `fluxo.resolvidoEm`: AVISO" do item 12) não derruba nada — a frase repete a regra do passo 2 da pré-conferência, que é quem governa; (2) remover o bloco C10 não é pego por nenhum grep; (3) o implementador não rodou a conferência headless do gate.
**Gate de fase medido pelo revisor** (claude 2.1.282, `claude -p --plugin-dir … --strict-mcp-config --mcp-config '{"mcpServers":{}}' --permission-mode plan`, fixtures em `mktemp -d`): design sem `Epic:` sem onboarding → AVISO · com `fluxo.resolvidoEm` → NÃO CONFORME com "fio condutor (R19)" FALTA · plano com T2/T3 paralelas em `prisma/` → "Contenda (R21)" FALTA.
Ruling F5-R11 — T23 fecha sem rodada de correção: (3) a omissão veio da instrução do orquestrador no dispatch ("descreva… a prova é pela leitura"), erro meu; o gate agora está medido de verdade; (1) conflito com o plano: a mutação prescrita mira uma frase redundante; o comportamento é do passo 2 e a execução headless o prova — a skill está certa, a mutação do plano é que está mal apontada; (2) C10 sem rede é lacuna de cobertura (mesma classe de F4-R7/F5-R10) → `ci.yml` de T25 ganha `grep -c 'Ela DELEGA e depois CONFERE'` = 1 e o grep do passo 2 da pré-conferência sobre `skills/gabarito-conformidade/SKILL.md` — custo-se-errado: C10 ou a regra de AVISO podem sumir sem alarme até T25.
Minor diferido (T25): nenhuma checagem mantém `PULL_REQUEST_TEMPLATE.md` em sincronia com `tiposLivres` (`versionamento-check.mjs:28`).


Review final da branch (opus): REPROVADO por 1 MAJOR — a conformidade exigia `status` ∈ `fluxo.status` e reprovava card em status legítimo sem chave (Epic em `Backlog` → NÃO CONFORME com emenda "trocar por Preparado"; Iniciativa em `Funil` → SUSPEITA "defina FL3"), contra a Emenda #7 do plano-mestre. Suítes verdes; o que o onboarding grava bate com o que cartão, doctor, `versionamento-check` e os hooks leem (conferido com `--set` real em fixture). Minors: MINOR-2 cartão lê `epic` fixo (confirmado em `cartao-sessao.mjs:228-229`); MINOR-3 `em` do cache em UTC (F2-R10); MINOR-4 exceção serial do Isolamento só no agente; MINOR-5 conformidade cita `fluxo.md §5` para o fio condutor; MINOR-6 contratos entre arquivos só em texto (4 mutações sobrevivem); MINOR-7 `"mcp": "<… ou null>"` entre aspas; MINOR-8/9/10 e 2 suspeitas.
Ruling F5-R12 — uma rodada (sonnet) para MAJOR-1, MINOR-2 (em `arquivos`, vínculo fixo `{"tipo":"frontmatter","nome":"epic"}` sem pergunta) e MINOR-7 (`null` JSON) — custo-se-errado: card legítimo reprovado e onboarding gravando `"null"`.
Fix 1d45932. Re-review (sonnet) APROVADO, medido em headless: Epic em `Backlog` e Iniciativa em `Funil` → CONFORME; reverter o fix reproduz o NÃO CONFORME; Epic em `Preparado` sem PBIs continua reprovado pelo item certo; status com erro de digitação (`Em Execuçao`) continua FALTA — a regra nova não virou brecha.
Handoff: T25 — MINOR-3, 4, 5, 6 (cinco checagens de `ci.yml`: chaves do cache no snippet do `--vincular`, rótulos do Apêndice × `AGENTS.md`, títulos de `fluxo.md` citados, `diff` do bloco Isolamento, marcador do CODEOWNERS × `instalar.sh`), greps de F5-R10/F5-R11, `cartao-sessao.mjs` lendo `vinculoPbiEpic.nome`, `plugin.json` 1.1.0 (hoje o cartão pede `--atualizar` já numa instalação nova), job novo no ruleset de `main`. Backlog — MINOR-8 (`VINCULO` sem dedupe e fora de `referencia.md §2.3`; stack trace no `wx` do CODEOWNERS), MINOR-9 (refazer onboarding com Apêndice já preenchido; CODEOWNERS do onboarding sem marcador), MINOR-10 (`Bash(bash:*)` largo), suspeitas (`claude mcp list` faz health-check antes do ok; prefixo de branch para `release`/`revert`); candidato do plano: "conformidade lê card direto da ferramenta por MCP" — dono Gabriel, gatilho 3 pedidos de card colado à mão. T26 — onboarding ponta a ponta com AskUserQuestion e MCP real.
Gate da fase: 4 arquivos do plano (2 SKILL.md + agente + comando) + 1 extra (PR template, F5-R6b) · `validate --strict` ok · conferência headless (1) AVISO (2) FALTA fio condutor (3) contenda (R21) FALTA.

## CORTE DA SESSÃO (com motivo)

## FECHO — PR mergeada
